# 📚 파이썬 `list.sort()` 완전 정리

`list.sort()`는 파이썬에서 리스트를 정렬할 때 사용하는 대표적인 메서드입니다.  
정렬 알고리즘의 특징, 사용법, 주의점 등을 한번에 정리한 문서입니다.

---

# 🔍 1. `list.sort()`란?

`sort()`는 **리스트 자체를 정렬(in-place)**하는 메서드입니다.

```python
numbers = [3, 1, 2]
numbers.sort()
print(numbers)  # [1, 2, 3]
